import { View } from "react-native";

const Separator: React.FC = () => {
    return <View style={{
        height: 1,
        width: 300,
        backgroundColor: "gray",
    }}/>
}

export default Separator;